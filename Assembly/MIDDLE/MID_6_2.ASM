.MODEL	SMALL
.STACK	100H
.CODE
MAIN	PROC
	MOV	AH,2
	MOV	DL,'?'
	INT	21H
	
	MOV	BH,0
	MOV	AH,1
@WHILE:
	INT	21H
	CMP	AL,0DH
	JE	@END_WHILE

	CMP	AL,'1'
	JB	@NEXT
	CMP	AL,'9'
	JBE	@ADD_COUNT
	CMP	AL,'A'
	JB	@NEXT
	CMP	AL,'Z'
	JBE	@ADD_COUNT
	CMP	AL,'a'
	JB	@NEXT
	CMP	AL,'z'
	JA	@NEXT
@ADD_COUNT:
	INC	BH
@NEXT:
	JMP	@WHILE

@END_WHILE:
	MOV	AH,2
	MOV	DL,0DH
	INT	21H
	MOV	DL,0AH
	INT	21H
	MOV	DL,BH
	ADD	DL,30H
	INT	21H

	MOV	AH,4CH
	INT	21H
MAIN	ENDP
	END	MAIN